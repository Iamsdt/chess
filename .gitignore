node_modules/
dist/
coverage/
.env
.env.*
!.env.example
playwright-report/
test-results/
.DS_Store
*.tsbuildinfo

# Review artefacts from `npm run screenshots` — regenerate on demand rather than
# storing ~6 MB of PNGs per sprint in git history; attach them to the PR instead.
docs/screenshots/
